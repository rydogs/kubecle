{
  "name": "kubecle",
  "version": "1.0.0",
  "description": "Useful ui for kubernettes",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production npm --prefix client run build && node server/index",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "NODE_ENV=development nodemon server/index"
  },
  "keywords": [
    "kubernettes",
    "k8s"
  ],
  "author": "Ryan Liu",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "express-async-handler": "^1.1.3",
    "kubernetes-client": "^5.3.1"
  },
  "devDependencies": {
    "nodemon": "^1.17.5"
  }
}
